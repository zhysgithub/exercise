#ifndef SERVER_H
#define SERVER_H

#include <QMainWindow>
#include <QWidget>

class server : public QMainWindow
{
    Q_OBJECT
public:
    explicit server(QWidget *parent = 0);

signals:

public slots:
};

#endif // SERVER_H